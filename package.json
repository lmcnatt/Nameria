{
  "name": "Nameria",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "load-data": "AWS_PROFILE=personal AWS_REGION=us-west-2 node scripts/load-data.js nameria-dnd-species",
    "sync-images": "AWS_PROFILE=personal aws s3 sync frontend/images/ s3://nameria-dnd-website-825551243283/images/ --region us-west-2",
    "sync-frontend": "AWS_PROFILE=personal aws s3 sync frontend/ s3://nameria-dnd-website-825551243283/ --region us-west-2",
    "invalidate-cache": "AWS_PROFILE=personal aws cloudfront create-invalidation --distribution-id E154XQH7TOLHN2 --paths '/*' --region us-west-2",
    "deploy-frontend": "pnpm sync-frontend && pnpm invalidate-cache"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.17.1",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.943.0",
    "@aws-sdk/lib-dynamodb": "^3.943.0"
  }
}
